// Enum used for the different sidebar item
export enum sideBarItemType {
  BASE_TABLE,
  DASHBOARD,
}

// Enum used for the different modals for the dashboard management menu
export enum modalType {
  ADD,
  REMOVE,
}

// Enum used for the different workspace types
export enum workspaceType {
  EMPTY,
  BASE_TABLE,
  DISPLAY_DASHBOARD,
  EDIT_DASHBOARD,
}

// Enum used for the different possbile elements in a dashboard
export enum elementType {
  GRIDVIEW,
  STATIC,
}

// Enum used for the different loading states of grid views
export enum loadingState {
  LOADING,
  READY,
}

// Enum used for the state of the sider menu
export enum siderMenuState {
  READY,
  LOADING,
}
