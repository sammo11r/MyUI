- name: allowed-queries
  definition:
    queries:
      - name: Get all users
        query: |
          query MyQuery {
          	users {
          		id
          		name
          		password
          	}
          }
