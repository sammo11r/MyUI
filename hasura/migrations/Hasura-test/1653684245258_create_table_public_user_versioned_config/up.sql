CREATE TABLE "public"."user_versioned_config" ("user_id" integer NOT NULL, "date" timestamptz NOT NULL DEFAULT now(), "config" json NOT NULL, PRIMARY KEY ("user_id","date") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict);
